name: Deploy PWA to Pages

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Build with Next.js
        env:
          NEXT_PUBLIC_BUSINESS_NAME: "Top Notch Pet Spa"
          NEXT_PUBLIC_BUSINESS_CITY: "New York, NY"
          NEXT_PUBLIC_THEME_COLOR: "#14b8a6"
          NEXT_PUBLIC_BUSINESS_SLUG: "top-notch-pet-spa"
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
        run: |
          # The Automation Magic: 
          # We set the Base Path to the current Repo Name
          export NEXT_PUBLIC_BASE_PATH=/${{ github.event.repository.name }}
          npx next build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
